extends layout
block content
  div.my-3
    p ユーザー情報
    form(method="post", action=`/users/${user.userId}/edit1`)
      div.form-group
        label(for="name") ユーザーネーム
        input(type="text" name="name" value=user.username)#name.form-control
      div
        button(type="submit").btn.btn-info 変更する
